/// Copy this file to `openrouter_embedded.dart` and paste your OpenRouter key.
/// `openrouter_embedded.dart` is gitignored so the key is not pushed to GitHub.
const String kEmbeddedOpenRouterApiKey = '';
